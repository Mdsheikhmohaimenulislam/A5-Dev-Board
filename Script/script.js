document.getElementById('card-btn').addEventListener('click',function(event){
    event.preventDefault();

    const taskAssigend = document.getElementById('task-assigend').innerText;
    const convertTaskAssigend = parseFloat(taskAssigend);


    const increment = 1
    const convertIncrement = parseFloat(increment)

 
    
    
    const addNumber = document.getElementById('add-number').innerText;
    const convertAddNumber = parseFloat(addNumber);

    if(convertTaskAssigend){
        alert("Board Updated Successfully");
        const sum = convertTaskAssigend - convertIncrement;
        document.getElementById('task-assigend').innerText = sum;

        if(convertAddNumber){

            const sum = convertAddNumber + convertIncrement;
            document.getElementById('add-number').innerText = sum;
            

            if(convertAddNumber){

               const time = new Date().toLocaleTimeString();

               const currentTime = document.getElementById('show-sms');
               
               const storTime = document.createElement('storTime');
               
               storTime.innerHTML = `<h3 class="bg-[#EDF1FC] p-2">You have completed the task, Fix Mobile Button Issue  ${time} </h3>`;
               
               currentTime.appendChild(storTime);

            
                document.getElementById('card-btn').disabled = true;
            }
            else{
                alert('Invalid code');
            }
        }
        else{
            alert('Invalid code');
        }
    }
    else{
        alert('Invalid code');
    }
    

    

});


// Second card


document.getElementById('second-btn').addEventListener('click',function(event){
    event.preventDefault();

    const taskAssigend = document.getElementById('task-assigend').innerText;
    const convertTaskAssigend = parseFloat(taskAssigend);


    const increment = 1
    const convertIncrement = parseFloat(increment)

 
    
    
    const addNumber = document.getElementById('add-number').innerText;
    const convertAddNumber = parseFloat(addNumber);

    if(convertTaskAssigend){
        alert("Board Updated Successfully");
        const sum = convertTaskAssigend - convertIncrement;
        document.getElementById('task-assigend').innerText = sum;

        if(convertAddNumber){

            const sum = convertAddNumber + convertIncrement;
            document.getElementById('add-number').innerText = sum;
            

            if(convertAddNumber){

               const time = new Date().toLocaleTimeString();

               const currentTime = document.getElementById('show-sms');
               
               const storTime = document.createElement('storTime');
               
               storTime.innerHTML = `<h3 class="bg-[#EDF1FC] p-2">You have completed the task, Add Dark Mode ${time} </h3>`;
               
               currentTime.appendChild(storTime);
            
                document.getElementById('second-btn').disabled = true;
            }
            else{
                alert('Invalid code');
            }
        }
        else{
            alert('Invalid code');
        }
    }
    else{
        alert('Invalid code');
    }
    

    

});

// Third card..

document.getElementById('third-btn').addEventListener('click',function(event){
    event.preventDefault();

    const taskAssigend = document.getElementById('task-assigend').innerText;
    const convertTaskAssigend = parseFloat(taskAssigend);


    const increment = 1
    const convertIncrement = parseFloat(increment)

 
    
    
    const addNumber = document.getElementById('add-number').innerText;
    const convertAddNumber = parseFloat(addNumber);

    if(convertTaskAssigend){
        alert("Board Updated Successfully");
        const sum = convertTaskAssigend - convertIncrement;
        document.getElementById('task-assigend').innerText = sum;

        if(convertAddNumber){

            const sum = convertAddNumber + convertIncrement;
            document.getElementById('add-number').innerText = sum;
            

            if(convertAddNumber){

               const time = new Date().toLocaleTimeString();

               const currentTime = document.getElementById('show-sms');
               
               const storTime = document.createElement('storTime');
               
               storTime.innerHTML = `<h3 class="bg-[#EDF1FC] p-2">You have completed the task, Optimize Home page   ${time} </h3>`;
               
               currentTime.appendChild(storTime);
            
                document.getElementById('third-btn').disabled = true;
            }
            else{
                alert('Invalid code');
            }
        }
        else{
            alert('Invalid code');
        }
    }
    else{
        alert('Invalid code');
    }
    

    

});


// Fourth card..


document.getElementById('fourth-btn').addEventListener('click',function(event){
    event.preventDefault();

    const taskAssigend = document.getElementById('task-assigend').innerText;
    const convertTaskAssigend = parseFloat(taskAssigend);


    const increment = 1
    const convertIncrement = parseFloat(increment)

 
    
    
    const addNumber = document.getElementById('add-number').innerText;
    const convertAddNumber = parseFloat(addNumber);

    if(convertTaskAssigend){
        alert("Board Updated Successfully");
        const sum = convertTaskAssigend - convertIncrement;
        document.getElementById('task-assigend').innerText = sum;

        if(convertAddNumber){

            const sum = convertAddNumber + convertIncrement;
            document.getElementById('add-number').innerText = sum;
            

            if(convertAddNumber){

               const time = new Date().toLocaleTimeString();

               const currentTime = document.getElementById('show-sms');
               
               const storTime = document.createElement('storTime');
               
               storTime.innerHTML = `<h3 class="bg-[#EDF1FC] p-2">You have completed the task, Add new emoji 🤲  ${time} </h3>`;
               
               currentTime.appendChild(storTime);
            
                document.getElementById('fourth-btn').disabled = true;
            }
            else{
                alert('Invalid code');
            }
        }
        else{
            alert('Invalid code');
        }
    }
    else{
        alert('Invalid code');
    }
    

    

});


// Fifth card..

document.getElementById('fifth-btn').addEventListener('click',function(event){
    event.preventDefault();

    const taskAssigend = document.getElementById('task-assigend').innerText;
    const convertTaskAssigend = parseFloat(taskAssigend);


    const increment = 1
    const convertIncrement = parseFloat(increment)

 
    
    
    const addNumber = document.getElementById('add-number').innerText;
    const convertAddNumber = parseFloat(addNumber);

    if(convertTaskAssigend){
        alert("Board Updated Successfully");
        const sum = convertTaskAssigend - convertIncrement;
        document.getElementById('task-assigend').innerText = sum;

        if(convertAddNumber){

            const sum = convertAddNumber + convertIncrement;
            document.getElementById('add-number').innerText = sum;
            

            if(convertAddNumber){

               const time = new Date().toLocaleTimeString();

               const currentTime = document.getElementById('show-sms');
               
               const storTime = document.createElement('storTime');
               
               storTime.innerHTML = `<h3 class="bg-[#EDF1FC] p-2">You have completed the task, Integrate OpenAI API ${time} </h3>`;
               
               currentTime.appendChild(storTime);
            
                document.getElementById('fifth-btn').disabled = true;
            }
            else{
                alert('Invalid code');
            }
        }
        else{
            alert('Invalid code');
        }
    }
    else{
        alert('Invalid code');
    }
    

    

});

// Sixth card..

document.getElementById('sixth-btn').addEventListener('click',function(event){
    event.preventDefault();

    const taskAssigend = document.getElementById('task-assigend').innerText;
    const convertTaskAssigend = parseFloat(taskAssigend);


    const increment = 1
    const convertIncrement = parseFloat(increment)

 
    
    
    const addNumber = document.getElementById('add-number').innerText;
    const convertAddNumber = parseFloat(addNumber);

    if(convertTaskAssigend){
        alert("Board Updated Successfully");
        const sum = convertTaskAssigend - convertIncrement;
        document.getElementById('task-assigend').innerText = sum;

        if(convertAddNumber){

            const sum = convertAddNumber + convertIncrement;
            document.getElementById('add-number').innerText = sum;
            

            if(convertAddNumber){

               const time = new Date().toLocaleTimeString();

               const currentTime = document.getElementById('show-sms');
               
               const storTime = document.createElement('storTime');
               
               storTime.innerHTML = `<h3 class="bg-[#EDF1FC] p-2">You have completed the task, Improve Job searching ${time} </h3>`;
               
               currentTime.appendChild(storTime);
            
                document.getElementById('sixth-btn').disabled = true;
            }
            else{
                alert('Invalid code');
            }
        }
        else{
            alert('Invalid code');
        }
    }
    else{
        alert('Invalid code');
    }
    

    

});